#- name: Expose ports on docker machine
#  hosts: localhost
#  connection: local
#  gather_facts: no
#  tasks: 
#    - shell: VBoxManage controlvm "default" natpf1 "tcp-port8000,tcp,,8000,,8000"
#      when: docker_machine == 'yes'
#      ignore_errors: yes
#    - shell: VBoxManage controlvm "default" natpf1 "tcp-port3306,tcp,,3306,,3306"
#      when: docker_machine == 'yes'
#      ignore_errors: yes

- name: Bring up the lnmp stack
  hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - { role: docker_lnmp_stack }  
